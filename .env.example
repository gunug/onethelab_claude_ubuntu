# Google OAuth 설정
# Google Cloud Console에서 OAuth 2.0 클라이언트 ID 생성 후 입력
# https://console.cloud.google.com/apis/credentials

GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret

# 허용된 이메일 목록 (쉼표로 구분)
# 비워두면 모든 Google 계정 허용 (보안 위험!)
ALLOWED_EMAILS=admin@example.com,user@example.com

# 세션 암호화 키 (랜덤 문자열, 32자 이상 권장)
# 생성 방법: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
SESSION_SECRET=your-random-secret-key-here
